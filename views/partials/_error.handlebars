<!-- handle the error messages when render the page, 
     errors is an array contains multiple error messages -->
{{#each errors}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<!-- 8. -->
<!-- handle error message from 'passportConfig.js' -->
{{#if fail_passport}}
<div class="alert alert-danger">{{fail_passport}}</div>
{{/if}}
<!-- end of 8. -->