========== modify 'Ideas.js' in models ==========
    
    === we want to know who is the author for the idea notes ===

1. in folder 'models': in 'Ideas.js': 
    add 1 more field called "userID" with 
        type : mongoose.Types.ObjectId,
        (do not add  reuqired:true  at this point )
    
2. in 'ideasController.js' : postAddIdea(): 
    add userID in newUser
    
3. try add a new idea, check whether the newly inserted idea contains  'userID' field with 'ObjectId'
    use note-dev
    db.ideas.find().pretty()


    === the user should only see the notes created by himself ===

4. in 'ideasController.js' : getIdeas(): 
    add the query of {userID : res.locals.user._id} inside find()

5. check whether the changes applied successfuly

    if success, the page will be :
        userA can only see noteA (created by userA)
        userB can only see noteB (created by userB)

6. clear the 'ideas' collection to prevent unexpected error
    db.ideas.drop()


7. modify 'Ideas.js' in models, in 'Ideas.js': 
    add 1 more property in field "userID": 
        required : true 



