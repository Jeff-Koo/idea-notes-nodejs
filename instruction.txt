========== display avatar on 'Profile' page ==========

1. in 'profile.handlebars': 
    use {{#if }} to check whether the current login user has avatar to show
        use add <img> to display avatar (if there is avatar)
        show message of "No Avatar" (if there is no avatar)

2. in 'usersController.js':  getProfile(): 
    add { avatar : res.locals.user.avatar } when render 'profile.handlebars' 


========== create a button to delete avatar ==========

3. in 'profile.handlebars': 
    make a form with the 'Delete Avatar' button

4. write delete route in 'usersRoute.js' for "/profile":
    router.delete("/profile", deleteProfile);
    remember to import { deleteProfile } from "../controllers/usersController.js"

5. write the function deleteProfile in 'usersController.js':
    export const deleteProfile = (req, res) => { ... };
   

========== delete the temporary files when upload avatar ==========
    upload avatar for multiple times, you will see there are multiple files in folder 'uploads'
    but the file in 'uploads' is only the temporary files, since the image has been stored in mongoDB

6. delete the temporary file in folder 'uploads' while storing the image into mongoDB
    in 'usersController.js':  postProfile() :
        fs.unlink();

    after clearing the folder 'uploads', the folder 'uploads' should keep empty even uploading avatar

7. remeber to protect the route 'profile', only user logged in can view this page
    in 'usersRoute.js': 
        import ensureAuthenticated from "../helpers/auth.js";
        add ensureAuthenticated to the following routes:
            get "/profile"      --> router.get("/profile", ensureAuthenticated, getProfile);
            post "/profile"
            delete "/profile"

8. check whether the functions work properly 

=========================================
========== END of the Template ==========
=========================================
