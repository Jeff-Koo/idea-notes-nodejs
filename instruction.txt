========== more partials for displaying messages ==========

1. inside folder 'views/partials': 
    create '_error.handlebars'

2. add {{> _error}} in 'main.handlebars'

3. remove the error message part in 'add.handlebars'

4. inside folder 'views/partials': create '_msg.handlebars'

5. add {{> _msg}} in 'main.handlebars'


========== making flash messages ==========

6. in 'app.js':
    import flash from "connect-flash";     (npm install connect-flash)
    import session from "express-session"; (npm install express-session)

7. in 'app.js':
    set up express-session 
    set up flash
    store 'success_msg' 'error_msg' in 'res.locals' of the express-session

8. call flash message in functions of 'ideasController.js'
   add  req.flash("xxx_msg", "sth")  before  res.redirect()  
      postAddIdea()
      putEditIdea()
      deleteIdea()
      
9. check whether the message shows up when AddIdea, EditIdea, DeleteIdea

