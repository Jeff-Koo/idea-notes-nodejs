========== prepare User Profile page for later upload avatar image ==========

1. in folder 'views/users':
    create 'profile.handlebars'
        show current user information
            - name
            - email
            - avatar (will show later)
            - HTML form for uploading avatar

2. in '_navbar.handlebars': 
    add hyperlink item for accessing 'profile' page
    <li>
        <a class="nav-link" href="/users/profile">Profile</a>
    </li>

3. write get route in 'usersRoute.js' for "/profile":
    router.get("/profile", getProfile);
    remember to import { getProfile } from "../controllers/usersController.js"

4. write the function getProfile in 'usersController.js':
    export const getProfile = (req, res) => { ... };

5. check whether the 'Profile' page render correctly.

6. modify 'Users.js' in folder 'models'
    add 1 more field called "avatar" with 2 fields inside "avatar"
        
        avatar : {
            data: {
                type : Buffer,
            }, 
            contentType: {
                type : String,
            }
        },
    
    * we are going to store image in mongoDB in format of Base64 (type : Buffer)

~ continue upload image in the next Step