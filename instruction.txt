========== making flash messages for edit notes ==========

1. in 'ideasController.js'
    check errors when edit the notes 
    edit codes in putEditIdea()
        "title" cannot be empty
        "details" cannot be empty

2. check empty before update value 
      
      set a string to store the error message

      if the string for error message is not empty
         error occurs, 
         show message, 
         redirect to edit page
      else 
         update value, 
         save to database, 
         show success message, 
         redirect to idea page
   

3. remove 'required' attribute of the <input> and <textarea> in 'edit.handlebars'

4. check if the error message shows 
   when the fields are empty when editing the note 
   