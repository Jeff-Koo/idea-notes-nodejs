========== set up Register function (continue) ==========

1. npm i bcryptjs (bcrypt also works)


2. in 'usersRoute.js': 
        import bcrypt from "bcryptjs";
        import User from "../models/Users.js";


=========== handle valid input data in register ===========

3. calculate the hash of the input password (using bcryptjs)
    - generate salt for hash 
    - hash password with the salt
    - save name, email, the calculated hash into mongoDB

4. show successful message and redirect to "/users/login" instead of "/"


5. if add user successfully, you can see the inserted document in mongoDB by using find() in collections 'users'
    use note-dev
    db.users.find().pretty()